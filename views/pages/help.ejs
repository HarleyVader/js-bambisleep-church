<%- include('../partials/header') %>

<main>
    <div class="container">
        <div class="help-layout">
            <!-- Sidebar Navigation -->
            <aside class="docs-sidebar">
                <h3>📚 Documentation</h3>
                <nav class="docs-nav">
                    <a href="/help" class="nav-item <%= !isMarkdown ? 'active' : '' %>">
                        🏠 Help Overview
                    </a>
                    <a href="/help/BambiSleepChurch" class="nav-item <%= (isMarkdown && currentDoc === 'BambiSleepChurch') ? 'active' : '' %>">
                        ⛪ Bambi Sleep Church
                    </a>
                    <a href="/help/smolagents-technical-guide" class="nav-item <%= (isMarkdown && currentDoc === 'smolagents-technical-guide') ? 'active' : '' %>">
                        🤖 Smolagents Technical Guide
                    </a>
                </nav>
            </aside>

            <!-- Main Content -->
            <section class="help-section">
                <% if (typeof isMarkdown !== 'undefined' && isMarkdown) { %>
                    <div class="<%= typeof isDocs !== 'undefined' && isDocs ? 'documentation-container' : 'markdown-content' %>">
                        <%- htmlContent %>
                    </div>
                <% } else { %>
                    <h2>Help & Documentation</h2>
                    <div class="help-content">
                    
                    <div class="help-item">
                        <h3>📝 How to Submit Content</h3>
                        <p>Use the <strong>Submit</strong> page to add new links or creator profiles to the community.</p>
                        <ul>
                            <li>Fill out all required fields</li>
                            <li>Choose the appropriate category</li>
                            <li>Add a clear description</li>
                            <li>Submit for community review</li>
                        </ul>
                    </div>

                    <div class="help-item">
                        <h3>📊 Understanding Stats</h3>
                        <p>The <strong>Stats</strong> page shows community metrics and popular content.</p>
                        <ul>
                            <li>View top-voted content</li>
                            <li>See community growth trends</li>
                            <li>Check platform statistics</li>
                        </ul>
                    </div>

                    <div class="help-item">
                        <h3>🏠 Navigation</h3>
                        <p>Use the main navigation to explore different sections:</p>
                        <ul>
                            <li><strong>Home</strong> - Overview and featured content</li>
                            <li><strong>Feed</strong> - Latest community submissions</li>
                            <li><strong>Submit</strong> - Add new content</li>
                            <li><strong>Stats</strong> - Community metrics</li>
                            <li><strong>Help</strong> - This documentation</li>
                        </ul>
                    </div>

                    <div class="help-item">
                        <h3>🗳️ Voting System</h3>
                        <p>Help the community by voting on content quality:</p>
                        <ul>
                            <li>Upvote quality content</li>
                            <li>Help surface the best submissions</li>
                            <li>Contribute to community curation</li>
                        </ul>                    </div>

                <div class="help-item">
                    <h3>📱 Platform Support</h3>
                    <p>We support content from various platforms:</p>
                    <ul>
                        <li>YouTube creators and videos</li>
                        <li>Discord communities</li>
                        <li>Reddit posts and communities</li>
                        <li>General web links</li>
                    </ul>
                </div>                </div>
            <% } %>
        </section>
        </div>
    </div>
</main>

<style>
.help-layout {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
}

/* Sidebar Styles */
.docs-sidebar {
    background: var(--card-background, #1a1a2e);
    padding: 1.5rem;
    border-radius: 8px;
    border: 1px solid var(--border-color, #333);
    height: fit-content;
    position: sticky;
    top: 2rem;
}

.docs-sidebar h3 {
    margin-top: 0;
    margin-bottom: 1rem;
    color: var(--primary-color, #ff6b9d);
    font-size: 1.1rem;
    border-bottom: 1px solid var(--border-color, #333);
    padding-bottom: 0.5rem;
}

.docs-nav {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.nav-item {
    display: block;
    padding: 0.75rem 1rem;
    color: var(--text-color, #fff);
    text-decoration: none;
    border-radius: 6px;
    transition: all 0.3s ease;
    border: 1px solid transparent;
    font-size: 0.9rem;
}

.nav-item:hover {
    background: var(--hover-background, #2a2a4e);
    border-color: var(--primary-color, #ff6b9d);
    transform: translateX(4px);
}

.nav-item.active {
    background: var(--primary-color, #ff6b9d);
    color: var(--background-color, #0f0f23);
    font-weight: bold;
}

/* Responsive Design */
@media (max-width: 768px) {
    .help-layout {
        grid-template-columns: 1fr;
        gap: 1rem;
        padding: 1rem;
    }
    
    .docs-sidebar {
        position: static;
        order: 2;
    }
    
    .help-section {
        order: 1;
    }
}

.help-section {
    background: var(--card-background, #1a1a2e);
    padding: 2rem;
    border-radius: 8px;
    border: 1px solid var(--border-color, #333);
}

.help-content {
    display: grid;
    gap: 2rem;
}

.help-item {
    background: var(--card-background, #1a1a2e);
    padding: 1.5rem;
    border-radius: 8px;
    border: 1px solid var(--border-color, #333);
}

.help-item h3 {
    margin-top: 0;
    color: var(--primary-color, #ff6b9d);
    font-size: 1.25rem;
}

.help-item ul {
    margin-left: 1rem;
}

.help-item li {
    margin-bottom: 0.5rem;
    line-height: 1.4;
}

/* Markdown content styling */
.markdown-content, .documentation-container {
    line-height: 1.6;
    color: var(--text-color, #fff);
}

.markdown-content h1, .markdown-content h2, .markdown-content h3,
.documentation-container h1, .documentation-container h2, .documentation-container h3 {
    color: var(--primary-color, #ff6b9d);
    margin-top: 1.5rem;
    margin-bottom: 1rem;
}

.markdown-content h1:first-child, .markdown-content h2:first-child,
.documentation-container h1:first-child, .documentation-container h2:first-child {
    margin-top: 0;
}

.markdown-content ul, .documentation-container ul {
    margin-left: 1rem;
    margin-bottom: 1rem;
}

.markdown-content li, .documentation-container li {
    margin-bottom: 0.5rem;
}

.markdown-content p, .documentation-container p {
    margin-bottom: 1rem;
}

.markdown-content code, .documentation-container code {
    background: var(--code-background, #2a2a2a);
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    font-family: 'Courier New', monospace;
    color: var(--primary-color, #ff6b9d);
}

.markdown-content pre, .documentation-container pre {
    background: var(--code-background, #2a2a2a);
    padding: 1rem;
    border-radius: 8px;
    overflow-x: auto;
    margin: 1rem 0;
    border: 1px solid var(--border-color, #333);
}

.markdown-content blockquote, .documentation-container blockquote {
    border-left: 4px solid var(--primary-color, #ff6b9d);
    margin: 1rem 0;
    padding-left: 1rem;
    font-style: italic;
    background: var(--code-background, #2a2a2a);
    padding: 1rem;
    border-radius: 4px;
}

.markdown-content table, .documentation-container table {
    width: 100%;
    border-collapse: collapse;
    margin: 1rem 0;
}

.markdown-content th, .markdown-content td,
.documentation-container th, .documentation-container td {
    border: 1px solid var(--border-color, #333);
    padding: 0.5rem;
    text-align: left;
}

.markdown-content th, .documentation-container th {
    background: var(--primary-color, #ff6b9d);
    color: var(--background-color, #0f0f23);
    font-weight: bold;
}
</style>

<%- include('../partials/footer') %>
