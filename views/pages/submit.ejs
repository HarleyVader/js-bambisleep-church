<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submit Content - Bambi Sleep Church</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .unified-submit {
            max-width: 800px;
            margin: 0 auto;
            padding: 2rem;
        }

        .submit-card {
            background: rgba(var(--color-bg-secondary-rgb), 0.9);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 1rem;
            padding: 2rem;
            backdrop-filter: blur(10px);
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: var(--color-primary-cyan);
            font-weight: 600;
        }

        .form-input {
            width: 100%;
            padding: 0.75rem;
            background: rgba(var(--color-bg-primary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 0.5rem;
            color: var(--color-text-primary);
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-input:focus {
            outline: none;
            border-color: var(--color-primary-magenta);
            box-shadow: 0 0 10px rgba(var(--color-primary-magenta-rgb), 0.3);
        }

        .url-validation {
            margin-top: 0.5rem;
            padding: 0.75rem;
            border-radius: 0.5rem;
            display: none;
        }

        .url-validation.validating {
            display: block;
            background: rgba(var(--color-primary-cyan-rgb), 0.1);
            border: 1px solid var(--color-primary-cyan);
            color: var(--color-primary-cyan);
        }

        .url-validation.valid {
            display: block;
            background: rgba(var(--color-primary-green-rgb), 0.1);
            border: 1px solid var(--color-primary-green);
            color: var(--color-primary-green);
        }

        .url-validation.invalid {
            display: block;
            background: rgba(var(--color-primary-yellow-rgb), 0.1);
            border: 1px solid var(--color-primary-yellow);
            color: var(--color-primary-yellow);
        }

        .metadata-preview {
            margin-top: 1rem;
            padding: 1rem;
            background: rgba(var(--color-bg-secondary-rgb), 0.5);
            border-radius: 0.5rem;
            border: 1px solid var(--color-primary-cyan);
        }

        .metadata-preview h4 {
            margin: 0 0 0.5rem 0;
            color: var(--color-primary-cyan);
        }

        .metadata-grid {
            display: grid;
            grid-template-columns: auto 1fr;
            gap: 0.5rem;
            font-size: 0.875rem;
        }

        .metadata-label {
            color: var(--color-text-secondary);
            font-weight: 600;
        }

        .metadata-value {
            color: var(--color-text-primary);
        }

        .platform-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            padding: 0.25rem 0.5rem;
            background: rgba(var(--color-primary-magenta-rgb), 0.1);
            border: 1px solid var(--color-primary-magenta);
            border-radius: 0.75rem;
            color: var(--color-primary-magenta);
            font-size: 0.75rem;
            font-weight: 600;
        }

        .quality-score {
            float: right;
            padding: 0.25rem 0.5rem;
            border-radius: 0.5rem;
            font-size: 0.75rem;
            font-weight: 600;
        }

        .quality-score.high {
            background: rgba(var(--color-primary-green-rgb), 0.1);
            color: var(--color-primary-green);
        }

        .quality-score.medium {
            background: rgba(var(--color-primary-yellow-rgb), 0.1);
            color: var(--color-primary-yellow);
        }

        .quality-score.low {
            background: rgba(255, 0, 0, 0.1);
            color: #ff4444;
        }

        .submit-button {
            width: 100%;
            padding: 1rem;
            background: linear-gradient(135deg, var(--color-primary-magenta), var(--color-primary-cyan));
            border: none;
            border-radius: 0.5rem;
            color: white;
            font-size: 1.1rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 1rem;
        }

        .submit-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(var(--color-primary-magenta-rgb), 0.3);
        }

        .submit-button:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none;
        }

        .spinner {
            display: inline-block;
            width: 1rem;
            height: 1rem;
            border: 2px solid transparent;
            border-top: 2px solid currentColor;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-right: 0.5rem;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .error-message, .success-message {
            padding: 1rem;
            border-radius: 0.5rem;
            margin: 1rem 0;
            font-weight: 600;
        }

        .error-message {
            background: rgba(255, 0, 0, 0.1);
            border: 1px solid #ff4444;
            color: #ff4444;
        }

        .success-message {
            background: rgba(var(--color-primary-green-rgb), 0.1);
            border: 1px solid var(--color-primary-green);
            color: var(--color-primary-green);
        }

        /* Crawl Fetch Agent Styles */
        .form-hint {
            font-size: 0.75rem;
            color: var(--color-text-secondary);
            margin-top: 0.25rem;
            opacity: 0.8;
        }

        .crawl-options {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            margin-top: 0.5rem;
        }

        .checkbox-label {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.875rem;
            color: var(--color-text-secondary);
            cursor: pointer;
        }        .checkbox-label input[type="checkbox"] {
            accent-color: var(--color-primary-cyan);
        }

        .advanced-settings {
            background: rgba(var(--color-bg-primary-rgb), 0.3);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 0.5rem;
            padding: 1rem;
            margin-top: 0.5rem;
        }

        .setting-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.75rem;
        }

        .setting-row:last-child {
            margin-bottom: 0;
        }

        .setting-row label {
            flex: 1;
            margin-bottom: 0;
            font-size: 0.9rem;
        }

        .setting-row .form-input {
            flex: 0 0 150px;
            margin-left: 1rem;
        }

        .crawl-progress {
            background: rgba(var(--color-bg-secondary-rgb), 0.9);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 1rem;
            padding: 2rem;
            margin-top: 2rem;
            backdrop-filter: blur(10px);
        }

        .progress-bar {
            width: 100%;
            height: 8px;
            background: rgba(var(--color-bg-primary-rgb), 0.5);
            border-radius: 4px;
            overflow: hidden;
            margin: 1rem 0;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--color-primary-cyan), var(--color-primary-magenta));
            border-radius: 4px;
            transition: width 0.3s ease;
            width: 0%;
        }

        .progress-info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.875rem;
            color: var(--color-text-secondary);
        }

        .crawl-log {
            background: rgba(var(--color-bg-primary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 0.5rem;
            padding: 1rem;
            margin-top: 1rem;
            max-height: 200px;
            overflow-y: auto;
            font-family: 'Courier New', monospace;
            font-size: 0.75rem;
            color: var(--color-primary-cyan);
        }

        .feed-results {
            background: rgba(var(--color-bg-secondary-rgb), 0.9);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 1rem;
            padding: 2rem;
            margin-top: 2rem;
            backdrop-filter: blur(10px);
        }

        .feed-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .feed-controls {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .action-btn {
            padding: 0.5rem 1rem;
            background: linear-gradient(45deg, var(--color-primary-cyan), var(--color-primary-magenta));
            color: var(--color-bg-primary);
            text-decoration: none;
            border: none;
            border-radius: 0.5rem;
            font-size: 0.875rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .action-btn:hover {
            transform: translateY(-1px);
            box-shadow: 0 5px 15px rgba(var(--color-primary-magenta-rgb), 0.3);
        }

        .action-btn.secondary {
            background: rgba(var(--color-bg-secondary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            color: var(--color-primary-cyan);
        }

        .action-btn.secondary:hover {
            background: var(--color-primary-cyan);
            color: var(--color-bg-primary);
        }

        .feed-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .stat-item {
            background: rgba(var(--color-bg-primary-rgb), 0.5);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 0.5rem;
            padding: 1rem;
            text-align: center;
        }

        .stat-number {
            display: block;
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--color-primary-cyan);
            margin-bottom: 0.25rem;
        }

        .stat-label {
            font-size: 0.75rem;
            color: var(--color-text-secondary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .feed-container {
            min-height: 200px;
        }

        .feed-container.grid-view {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 1.5rem;
        }

        .feed-container.list-view {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .media-card {
            background: rgba(var(--color-bg-secondary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 1rem;
            padding: 1.5rem;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .media-card:hover {
            border-color: var(--color-primary-magenta);
            box-shadow: 0 0 20px rgba(var(--color-primary-magenta-rgb), 0.3);
            transform: translateY(-2px);
        }

        .media-card.video {
            border-left: 4px solid #ff0000;
        }

        .media-card.audio {
            border-left: 4px solid #ff8800;
        }

        .media-card.image {
            border-left: 4px solid #00ff88;
        }

        .media-card.creator {
            border-left: 4px solid #ff4488;
        }

        .media-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1rem;
        }

        .media-platform {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.875rem;
            color: var(--color-primary-cyan);
            font-weight: 600;
        }

        .media-type-badge {
            padding: 0.25rem 0.5rem;
            background: rgba(var(--color-primary-magenta-rgb), 0.2);
            border: 1px solid var(--color-primary-magenta);
            border-radius: 0.75rem;
            font-size: 0.75rem;
            color: var(--color-primary-magenta);
            font-weight: 600;
        }

        .media-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--color-text-primary);
            margin-bottom: 0.5rem;
            line-height: 1.3;
        }

        .media-title a {
            color: inherit;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .media-title a:hover {
            color: var(--color-primary-cyan);
        }        .media-description {
            color: var(--color-text-secondary);
            font-size: 0.875rem;
            line-height: 1.4;
            margin-bottom: 1rem;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .media-player {
            margin: 1rem 0;
            border-radius: 0.5rem;
            overflow: hidden;
        }

        .media-player iframe {
            width: 100%;
            height: 200px;
            border: none;
        }

        .media-player audio {
            width: 100%;
        }

        .media-player video {
            width: 100%;
            max-height: 200px;
        }

        .media-player img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .media-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.75rem;
            color: var(--color-text-secondary);
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid rgba(var(--color-primary-cyan-rgb), 0.2);
        }

        .media-actions {
            display: flex;
            gap: 0.5rem;
        }

        .media-action-btn {
            padding: 0.25rem 0.5rem;
            background: none;
            border: 1px solid var(--color-primary-cyan);
            color: var(--color-primary-cyan);
            border-radius: 0.25rem;
            font-size: 0.75rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .media-action-btn:hover {
            background: var(--color-primary-cyan);
            color: var(--color-bg-primary);
        }        .media-action-btn.selected {
            background: var(--color-primary-cyan);
            color: var(--color-bg-primary);
        }

        /* Sitemap Visualization Styles */
        .sitemap-results {
            background: rgba(var(--color-bg-primary-rgb), 0.3);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 0.5rem;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }

        .sitemap-tabs {
            display: flex;
            gap: 0.5rem;
            margin-bottom: 1rem;
            border-bottom: 1px solid rgba(var(--color-primary-cyan-rgb), 0.3);
            padding-bottom: 0.5rem;
        }

        .sitemap-tab {
            padding: 0.5rem 1rem;
            background: none;
            border: 1px solid var(--color-primary-cyan);
            color: var(--color-primary-cyan);
            border-radius: 0.25rem;
            font-size: 0.875rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .sitemap-tab:hover,
        .sitemap-tab.active {
            background: var(--color-primary-cyan);
            color: var(--color-bg-primary);
        }

        .sitemap-view {
            display: none;
            min-height: 300px;
        }

        .sitemap-view.active {
            display: block;
        }

        .sitemap-hierarchy {
            font-family: 'Courier New', monospace;
            font-size: 0.875rem;
            color: var(--color-text-primary);
            line-height: 1.6;
            max-height: 400px;
            overflow-y: auto;
            background: rgba(var(--color-bg-secondary-rgb), 0.5);
            border-radius: 0.5rem;
            padding: 1rem;
        }

        .domain-item {
            margin-bottom: 1.5rem;
            border: 1px solid rgba(var(--color-primary-cyan-rgb), 0.3);
            border-radius: 0.5rem;
            padding: 1rem;
        }

        .domain-header {
            display: flex;
            justify-content: between;
            align-items: center;
            margin-bottom: 0.5rem;
            color: var(--color-primary-cyan);
            font-weight: 600;
        }

        .domain-stats {
            font-size: 0.75rem;
            color: var(--color-text-secondary);
        }

        .folder-tree {
            margin-left: 1rem;
            border-left: 1px dashed rgba(var(--color-primary-cyan-rgb), 0.3);
            padding-left: 1rem;
        }

        .page-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.25rem 0;
            font-size: 0.875rem;
        }

        .page-title {
            color: var(--color-text-primary);
        }

        .page-score {
            font-size: 0.75rem;
            padding: 0.125rem 0.375rem;
            border-radius: 0.25rem;
            font-weight: 600;
        }

        .page-score.bambisleep {
            background: rgba(var(--color-primary-magenta-rgb), 0.2);
            color: var(--color-primary-magenta);
        }

        .link-tree-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1rem;
            margin-bottom: 1rem;
        }

        .link-stat-item {
            background: rgba(var(--color-bg-secondary-rgb), 0.5);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 0.5rem;
            padding: 0.75rem;
            text-align: center;
        }

        .link-stat-number {
            display: block;
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--color-primary-cyan);
            margin-bottom: 0.25rem;
        }

        .link-stat-label {
            font-size: 0.75rem;
            color: var(--color-text-secondary);
        }

        .hub-page {
            background: rgba(var(--color-bg-secondary-rgb), 0.5);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 0.5rem;
            padding: 0.75rem;
            margin-bottom: 0.5rem;
        }

        .hub-page-title {
            font-weight: 600;
            color: var(--color-text-primary);
            margin-bottom: 0.25rem;
        }

        .hub-page-stats {
            font-size: 0.75rem;
            color: var(--color-text-secondary);
        }

        @media (max-width: 768px) {
            .feed-header {
                flex-direction: column;
                align-items: flex-start;
            }

            .feed-controls {
                width: 100%;
                justify-content: center;
            }

            .feed-container.grid-view {
                grid-template-columns: 1fr;
            }

            .crawl-options {
                flex-direction: column;
                gap: 0.5rem;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>
    
    <main>
        <div class="unified-submit">
            <h1 class="neon-text scan-line">Submit Content</h1>
            <p class="subtitle">Add any URL - we'll automatically detect the platform and organize it</p>
            
            <div class="submit-card">
                <form id="unifiedSubmitForm">
                    <div class="form-group">
                        <label for="url">Content URL *</label>
                        <input type="url" id="url" name="url" class="form-input" 
                               placeholder="https://youtube.com/watch?v=... or https://patreon.com/creator or any supported URL"
                               required>
                        
                        <div id="urlValidation" class="url-validation">
                            <div class="spinner"></div>
                            Checking URL and extracting metadata...
                        </div>
                        
                        <div id="metadataPreview" class="metadata-preview" style="display: none;">
                            <h4>Detected Content <span id="qualityScore" class="quality-score"></span></h4>
                            <div id="metadataGrid" class="metadata-grid"></div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="title">Title (optional - will use detected title if empty)</label>
                        <input type="text" id="title" name="title" class="form-input" 
                               placeholder="Leave empty to use automatically detected title">
                    </div>

                    <div class="form-group">
                        <label for="description">Description (optional - will use detected description if empty)</label>
                        <textarea id="description" name="description" class="form-input" rows="3"
                                  placeholder="Leave empty to use automatically detected description"></textarea>
                    </div>

                    <div id="messageContainer"></div>

                    <button type="submit" id="submitButton" class="submit-button" disabled>
                        Submit Content
                    </button>
                </form>
            </div>
        </div>

        <!-- Crawl Fetch Agent Section -->
        <div class="unified-submit" style="margin-top: 3rem;">
            <h2 class="neon-text scan-line">🕷️ Crawl Fetch Agent</h2>
            <p class="subtitle">Request batch URL crawling with instant feed preview</p>
            
            <div class="submit-card">
                <form id="crawlFetchForm">
                    <div class="form-group">
                        <label for="crawlUrls">URLs to Crawl *</label>
                        <textarea id="crawlUrls" name="crawlUrls" class="form-input" rows="4"
                                  placeholder="Enter one URL per line:&#10;https://youtube.com/playlist?list=...&#10;https://soundcloud.com/artist&#10;https://patreon.com/creator&#10;https://bambisleep.info/..."
                                  required></textarea>
                        <div class="form-hint">One URL per line • Supports playlists, creators, and content pages</div>
                    </div>                    <div class="form-group">
                        <label for="crawlOptions">Crawl Options</label>
                        <div class="crawl-options">
                            <label class="checkbox-label">
                                <input type="checkbox" id="respectRobots" checked> Respect robots.txt
                            </label>
                            <label class="checkbox-label">
                                <input type="checkbox" id="extractMedia" checked> Extract media files
                            </label>
                            <label class="checkbox-label">
                                <input type="checkbox" id="followLinks"> Follow internal links
                            </label>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="advancedMode">🚀 Advanced Mode</label>
                        <div class="crawl-options">
                            <label class="checkbox-label">
                                <input type="checkbox" id="enableAdvanced"> Enable advanced crawl with sitemap generation
                            </label>
                            <label class="checkbox-label">
                                <input type="checkbox" id="autoIndex"> Auto-index discovered bambisleep content
                            </label>
                            <label class="checkbox-label">
                                <input type="checkbox" id="generateSitemap"> Generate hierarchical sitemap
                            </label>
                        </div>
                    </div>

                    <div id="advancedOptions" class="form-group" style="display: none;">
                        <label>Advanced Crawl Settings</label>
                        <div class="advanced-settings">
                            <div class="setting-row">
                                <label for="maxDepth">Max Crawl Depth</label>
                                <select id="maxDepth" name="maxDepth" class="form-input">
                                    <option value="1">1 level (direct links only)</option>
                                    <option value="2" selected>2 levels</option>
                                    <option value="3">3 levels</option>
                                    <option value="4">4 levels (deep crawl)</option>
                                </select>
                            </div>
                            <div class="setting-row">
                                <label for="maxPages">Max Pages to Crawl</label>
                                <select id="maxPages" name="maxPages" class="form-input">
                                    <option value="25">25 pages</option>
                                    <option value="50" selected>50 pages</option>
                                    <option value="100">100 pages</option>
                                    <option value="200">200 pages</option>
                                </select>
                            </div>
                            <div class="setting-row">
                                <label for="crawlDelay">Crawl Delay (ms)</label>
                                <select id="crawlDelay" name="crawlDelay" class="form-input">
                                    <option value="500">500ms (fast)</option>
                                    <option value="1000" selected>1000ms (balanced)</option>
                                    <option value="2000">2000ms (respectful)</option>
                                    <option value="3000">3000ms (very slow)</option>
                                </select>
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="maxResults">Max Results per URL</label>
                        <select id="maxResults" name="maxResults" class="form-input">
                            <option value="10">10 items</option>
                            <option value="25" selected>25 items</option>
                            <option value="50">50 items</option>
                            <option value="100">100 items</option>
                        </select>
                    </div>

                    <div id="crawlMessageContainer"></div>

                    <button type="submit" id="crawlSubmitButton" class="submit-button">
                        🚀 Start Crawl & Generate Feed
                    </button>
                </form>
            </div>

            <!-- Crawl Progress & Feed Results -->
            <div id="crawlProgress" class="crawl-progress" style="display: none;">
                <h3>🔄 Crawl Progress</h3>
                <div class="progress-bar">
                    <div class="progress-fill" id="progressFill"></div>
                </div>
                <div class="progress-info">
                    <span id="progressText">Initializing...</span>
                    <span id="progressPercent">0%</</span>
                </div>
                <div id="crawlLog" class="crawl-log"></div>
            </div>            <!-- Feed Results -->
            <div id="feedResults" class="feed-results" style="display: none;">
                <div class="feed-header">
                    <h3>📡 Generated Feed</h3>
                    <div class="feed-controls">
                        <button id="toggleViewMode" class="action-btn secondary">🔲 Grid View</button>
                        <button id="bulkSubmit" class="action-btn">📤 Submit All to Community</button>
                        <button id="exportFeed" class="action-btn secondary">💾 Export JSON</button>
                        <button id="downloadSitemap" class="action-btn secondary" style="display: none;">🗺️ Download Sitemap</button>
                    </div>
                </div>
                
                <div class="feed-stats">
                    <div class="stat-item">
                        <span class="stat-number" id="totalFound">0</span>
                        <span class="stat-label">Items Found</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number" id="mediaCount">0</span>
                        <span class="stat-label">Media Files</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number" id="platformCount">0</span>
                        <span class="stat-label">Platforms</span>
                    </div>
                    <div class="stat-item" id="bambisleepStat" style="display: none;">
                        <span class="stat-number" id="bambisleepCount">0</span>
                        <span class="stat-label">Bambisleep Content</span>
                    </div>
                </div>

                <!-- Sitemap Visualization -->
                <div id="sitemapResults" class="sitemap-results" style="display: none;">
                    <h4>🗺️ Site Hierarchy & Link Tree</h4>
                    <div class="sitemap-tabs">
                        <button class="sitemap-tab active" data-tab="hierarchy">🏗️ Hierarchy</button>
                        <button class="sitemap-tab" data-tab="linktree">🌳 Link Tree</button>
                        <button class="sitemap-tab" data-tab="domains">🌐 Domains</button>
                    </div>
                    
                    <div id="hierarchyView" class="sitemap-view active">
                        <div id="sitemapHierarchy" class="sitemap-hierarchy"></div>
                    </div>
                    
                    <div id="linktreeView" class="sitemap-view">
                        <div id="linkTreeStats" class="link-tree-stats"></div>
                        <div id="linkTreeVisualization" class="link-tree-viz"></div>
                    </div>
                    
                    <div id="domainsView" class="sitemap-view">
                        <div id="domainAnalysis" class="domain-analysis"></div>
                    </div>
                </div>

                <div id="feedContainer" class="feed-container grid-view">
                    <!-- Dynamic content will be generated here -->
                </div>
            </div>
        </div>
    </main>
    <%- include('../partials/footer') %>
    <script src="/js/unified-submit.js"></script>
</body>
</html>
