<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enhanced Live Feed - Bambi Sleep Church</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/feed.css">
    <style>
        .enhanced-feed {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
        }
        
        .feed-header {
            text-align: center;
            margin-bottom: 2rem;
        }
        
        .feed-subtitle {
            font-size: 1.1rem;
            color: var(--color-text-secondary);
            margin-bottom: 1rem;
        }
        
        .feed-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 1rem;
            max-width: 600px;
            margin: 0 auto 2rem;
        }
        
        .stat-card {
            background: rgba(var(--color-bg-secondary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 0.75rem;
            padding: 1rem;
            text-align: center;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }
        
        .stat-card:hover {
            border-color: var(--color-primary-magenta);
            transform: translateY(-2px);
        }
        
        .stat-number {
            display: block;
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--color-primary-cyan);
            margin-bottom: 0.25rem;
        }
        
        .stat-label {
            font-size: 0.75rem;
            color: var(--color-text-secondary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .feed-controls {
            background: rgba(var(--color-bg-secondary-rgb), 0.6);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 1rem;
            padding: 1.5rem;
            margin-bottom: 2rem;
            backdrop-filter: blur(10px);
        }
        
        .controls-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1rem;
            flex-wrap: wrap;
            gap: 1rem;
        }
        
        .controls-title {
            color: var(--color-primary-cyan);
            font-size: 1.2rem;
            font-weight: 600;
            margin: 0;
        }
        
        .view-mode-toggle {
            display: flex;
            gap: 0.5rem;
        }
        
        .view-mode-btn {
            padding: 0.5rem 1rem;
            background: rgba(var(--color-bg-secondary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 0.5rem;
            color: var(--color-primary-cyan);
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 0.875rem;
        }
        
        .view-mode-btn:hover, .view-mode-btn.active {
            background: var(--color-primary-cyan);
            color: var(--color-bg-primary);
        }
        
        .filter-section {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 1rem;
            margin-bottom: 1rem;
        }
        
        .filter-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }
        
        .filter-label {
            font-size: 0.875rem;
            color: var(--color-text-secondary);
            font-weight: 600;
        }
        
        .filter-select, .filter-input {
            padding: 0.75rem;
            background: rgba(var(--color-bg-secondary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 0.5rem;
            color: var(--color-text-primary);
            font-size: 0.875rem;
        }
        
        .filter-select:focus, .filter-input:focus {
            outline: none;
            border-color: var(--color-primary-magenta);
            box-shadow: 0 0 10px rgba(var(--color-primary-magenta-rgb), 0.2);
        }
        
        .platform-filters {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            margin-top: 0.5rem;
        }
        
        .platform-filter-btn {
            padding: 0.25rem 0.75rem;
            background: rgba(var(--color-bg-secondary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 1rem;
            color: var(--color-primary-cyan);
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 0.75rem;
            font-weight: 600;
        }
        
        .platform-filter-btn:hover, .platform-filter-btn.active {
            background: var(--color-primary-cyan);
            color: var(--color-bg-primary);
        }
        
        .quick-actions {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }
        
        .action-btn {
            padding: 0.75rem 1.5rem;
            background: linear-gradient(45deg, var(--color-primary-cyan), var(--color-primary-magenta));
            color: var(--color-bg-primary);
            text-decoration: none;
            border-radius: 0.75rem;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .action-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(var(--color-primary-magenta-rgb), 0.3);
        }
        
        .action-btn.secondary {
            background: rgba(var(--color-bg-secondary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            color: var(--color-primary-cyan);
        }
        
        .action-btn.secondary:hover {
            background: var(--color-primary-cyan);
            color: var(--color-bg-primary);
        }
          .advanced-filters {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }
        
        .checkbox-label {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.875rem;
            color: var(--color-text-secondary);
            cursor: pointer;
        }
        
        .checkbox-label input[type="checkbox"] {
            accent-color: var(--color-primary-cyan);
        }
        
        .feed-view {
            transition: all 0.3s ease;
        }
        
        .platform-view .platform-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 1.5rem;
        }
        
        .platform-card {
            background: rgba(var(--color-bg-secondary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 1rem;
            padding: 1.5rem;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
            cursor: pointer;
        }
        
        .platform-card:hover {
            border-color: var(--color-primary-magenta);
            box-shadow: 0 0 20px rgba(var(--color-primary-magenta-rgb), 0.3);
            transform: translateY(-5px);
        }
        
        .platform-header {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-bottom: 1rem;
        }
        
        .platform-icon {
            font-size: 2rem;
        }
        
        .platform-name {
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--color-primary-cyan);
            text-transform: capitalize;
        }
        
        .platform-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(80px, 1fr));
            gap: 0.5rem;
            margin-bottom: 1rem;
            font-size: 0.875rem;
        }
        
        .platform-stat {
            text-align: center;
            padding: 0.5rem;
            background: rgba(var(--color-bg-secondary-rgb), 0.5);
            border-radius: 0.5rem;
            color: var(--color-text-secondary);
        }
        
        .platform-content-preview {
            max-height: 120px;
            overflow-y: auto;
            margin-top: 0.5rem;
        }
        
        .content-preview-item {
            padding: 0.25rem 0;
            border-bottom: 1px solid rgba(var(--color-primary-cyan-rgb), 0.1);
            font-size: 0.75rem;
        }
        
        .content-preview-item:last-child {
            border-bottom: none;
        }
        
        .stat-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.5rem;
            margin-bottom: 1rem;
        }
        
        .mini-stat {
            text-align: center;
            padding: 0.5rem;
            background: rgba(var(--color-bg-secondary-rgb), 0.5);
            border-radius: 0.5rem;
        }
        
        .mini-stat-number {
            display: block;
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--color-primary-cyan);
        }
        
        .mini-stat-label {
            font-size: 0.75rem;
            color: var(--color-text-secondary);
        }
        
        .quality-stats {
            font-size: 0.875rem;
        }
        
        .quality-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.5rem 0;
            border-bottom: 1px solid rgba(var(--color-primary-cyan-rgb), 0.1);
        }
        
        .quality-item:last-child {
            border-bottom: none;
        }
        
        .quality-label {
            color: var(--color-text-secondary);
        }
        
        .quality-value {
            color: var(--color-primary-cyan);
            font-weight: 600;
        }
        
        .view-mode-btn.active {
            background: var(--color-primary-cyan);
            color: var(--color-bg-primary);
        }
        
        .filter-select, .filter-input {
            width: 100%;
            padding: 0.5rem;
            background: rgba(var(--color-bg-secondary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 0.5rem;
            color: var(--color-text-primary);
            font-size: 0.875rem;
        }
        
        .filter-select:focus, .filter-input:focus {
            outline: none;
            border-color: var(--color-primary-magenta);
            box-shadow: 0 0 10px rgba(var(--color-primary-magenta-rgb), 0.3);
        }
        
        .filter-label {
            display: block;
            margin-bottom: 0.5rem;
            color: var(--color-text-secondary);
            font-size: 0.875rem;
            font-weight: 600;
        }
        
        .filter-group {
            margin-bottom: 1rem;
        }
          .filter-section {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-bottom: 1.5rem;
        }
        
        .feed-layout {
            display: grid;
            grid-template-columns: 1fr 300px;
            gap: 2rem;
            align-items: start;
        }
        
        .feed-main {
            min-height: 500px;
        }
        
        .feed-sidebar {
            background: rgba(var(--color-bg-secondary-rgb), 0.6);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 1rem;
            padding: 1.5rem;
            backdrop-filter: blur(10px);
            position: sticky;
            top: 2rem;
        }
        
        .sidebar-section {
            margin-bottom: 2rem;
        }
        
        .sidebar-section:last-child {
            margin-bottom: 0;
        }
        
        .sidebar-title {
            color: var(--color-primary-magenta);
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }
        
        .platform-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        
        .platform-item {
            padding: 0.5rem 0;
            border-bottom: 1px solid rgba(var(--color-primary-cyan-rgb), 0.1);
            display: flex;
            align-items: center;
            justify-content: space-between;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .platform-item:hover {
            color: var(--color-primary-cyan);
        }
        
        .platform-item:last-child {
            border-bottom: none;
        }
        
        .platform-name {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-weight: 500;
        }
        
        .platform-count {
            font-size: 0.75rem;
            color: var(--color-text-secondary);
            background: rgba(var(--color-primary-cyan-rgb), 0.1);
            padding: 0.125rem 0.5rem;
            border-radius: 1rem;
        }
        
        .real-time-indicator {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.875rem;
            color: var(--color-text-secondary);
            margin-bottom: 1rem;
        }
        
        .status-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: var(--color-primary-green);
            animation: pulse 2s infinite;
        }
        
        .status-dot.disconnected {
            background: var(--color-primary-yellow);
        }
        
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        
        .recent-activity {
            font-size: 0.875rem;
        }
        
        .activity-item {
            padding: 0.5rem 0;
            border-bottom: 1px solid rgba(var(--color-primary-cyan-rgb), 0.1);
        }
        
        .activity-item:last-child {
            border-bottom: none;
        }
        
        .activity-time {
            color: var(--color-text-secondary);
            font-size: 0.75rem;
        }
          @media (max-width: 768px) {
            .feed-layout {
                grid-template-columns: 1fr;
            }
            
            .feed-sidebar {
                position: static;
                order: -1;
            }
            
            .filter-section {
                grid-template-columns: 1fr;
            }
            
            .controls-header {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .view-mode-toggle {
                margin-top: 1rem;
            }
            
            .advanced-filters {
                flex-direction: column;
            }
        }
        
        /* Additional styles for enhanced features */
        .grid-view {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 1.5rem;
        }
        
        .list-view .link-card {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1rem;
            padding: 1rem;
        }
        
        .compact-view .link-card {
            padding: 0.75rem;
            margin-bottom: 0.5rem;
        }
        
        .compact-view .link-description {
            display: none;
        }
        
        .quality-badge {
            font-size: 0.75rem;
            padding: 0.125rem 0.5rem;
            border-radius: 1rem;
            font-weight: 600;
        }
        
        .quality-excellent {
            background: var(--color-primary-green);
            color: white;
        }
        
        .quality-good {
            background: var(--color-primary-cyan);
            color: white;
        }
        
        .quality-fair {
            background: var(--color-primary-yellow);
            color: black;
        }
        
        .quality-poor {
            background: var(--color-text-secondary);
            color: white;
        }
        
        .notification {
            animation: slideIn 0.3s ease;
        }
        
        @keyframes slideIn {
            from {
                transform: translateX(100%);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }
        
        .link-card {
            background: rgba(var(--color-bg-secondary-rgb), 0.8);
            border: 1px solid var(--color-primary-cyan);
            border-radius: 1rem;
            padding: 1.5rem;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }
        
        .link-card:hover {
            border-color: var(--color-primary-magenta);
            box-shadow: 0 0 20px rgba(var(--color-primary-magenta-rgb), 0.3);
            transform: translateY(-5px);
        }
        
        .link-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
        }
        
        .platform-info {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .link-stats {
            display: flex;
            gap: 0.75rem;
            font-size: 0.875rem;
            color: var(--color-text-secondary);
        }
        
        .link-title a {
            color: var(--color-primary-cyan);
            text-decoration: none;
            font-weight: 600;
        }
        
        .link-title a:hover {
            color: var(--color-primary-magenta);
        }
        
        .link-description {
            color: var(--color-text-secondary);
            margin: 0.5rem 0;
            line-height: 1.4;
        }
        
        .link-uploader {
            color: var(--color-text-secondary);
            font-size: 0.875rem;
            margin: 0.25rem 0;
        }
        
        .link-meta {
            display: flex;
            gap: 1rem;
            font-size: 0.75rem;
            color: var(--color-text-secondary);
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid rgba(var(--color-primary-cyan-rgb), 0.1);
        }
        
        .content-type {
            background: rgba(var(--color-primary-cyan-rgb), 0.1);
            padding: 0.125rem 0.5rem;
            border-radius: 0.25rem;
            text-transform: uppercase;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>
    
    <main>
        <div class="enhanced-feed">
            <div class="feed-header">
                <h1 class="neon-text scan-line">Enhanced Live Feed</h1>
                <p class="feed-subtitle">Real-time content discovery with platform organization</p>
                
                <!-- Enhanced Stats -->
                <div class="feed-stats">
                    <div class="stat-card">
                        <span class="stat-number"><%= totalLinks %></span>
                        <span class="stat-label">Total Content</span>
                    </div>
                    <div class="stat-card">
                        <span class="stat-number" id="platformCount">0</span>
                        <span class="stat-label">Platforms</span>
                    </div>
                    <div class="stat-card">
                        <span class="stat-number" id="activeFilters">0</span>
                        <span class="stat-label">Filters</span>
                    </div>
                    <div class="stat-card">
                        <span class="stat-number" id="onlineUsers">1</span>
                        <span class="stat-label">Online</span>
                    </div>
                </div>
            </div>            <!-- Enhanced Controls -->
            <div class="feed-controls">
                <div class="controls-header">
                    <h3 class="controls-title">Enhanced Filters & Organization</h3>
                    <div class="view-mode-toggle">
                        <button class="view-mode-btn active" data-mode="grid">🔲 Grid</button>
                        <button class="view-mode-btn" data-mode="list">📄 List</button>
                        <button class="view-mode-btn" data-mode="compact">📋 Compact</button>
                        <button class="view-mode-btn" data-mode="platform">🌐 Platform</button>
                    </div>
                </div>
                
                <div class="filter-section">
                    <div class="filter-group">
                        <label class="filter-label">Sort By</label>
                        <select class="filter-select" id="sortFilter">
                            <option value="newest">Newest First</option>
                            <option value="oldest">Oldest First</option>
                            <option value="votes">Most Voted</option>
                            <option value="comments">Most Comments</option>
                            <option value="views">Most Views</option>
                            <option value="platform">Platform A-Z</option>
                            <option value="quality">Quality Score</option>
                            <option value="engagement">Total Engagement</option>
                        </select>
                    </div>
                    
                    <div class="filter-group">
                        <label class="filter-label">Content Type</label>
                        <select class="filter-select" id="typeFilter">
                            <option value="">All Types</option>
                            <option value="video">Videos</option>
                            <option value="audio">Audio</option>
                            <option value="image">Images</option>
                            <option value="playlist">Playlists</option>
                            <option value="creator">Creators</option>
                            <option value="other">Other</option>
                        </select>
                    </div>
                    
                    <div class="filter-group">
                        <label class="filter-label">Platform Category</label>
                        <select class="filter-select" id="categoryFilter">
                            <option value="">All Categories</option>
                            <option value="video">Video Platforms</option>
                            <option value="audio">Audio Platforms</option>
                            <option value="creator">Creator Platforms</option>
                            <option value="hypno">Hypno Platforms</option>
                            <option value="social">Social Platforms</option>
                            <option value="storage">Storage Platforms</option>
                        </select>
                    </div>
                    
                    <div class="filter-group">
                        <label class="filter-label">Search</label>
                        <input type="text" class="filter-input" id="searchFilter" placeholder="Search titles, creators, platforms...">
                    </div>
                </div>
                
                <div class="filter-group">
                    <label class="filter-label">Platform Filters</label>
                    <div class="platform-filters" id="platformFilters">
                        <!-- Dynamically populated -->
                    </div>
                </div>
                
                <div class="filter-group">
                    <label class="filter-label">Advanced Filters</label>
                    <div class="advanced-filters">
                        <label class="checkbox-label">
                            <input type="checkbox" id="highQualityOnly"> Only High Quality (6+)
                        </label>
                        <label class="checkbox-label">
                            <input type="checkbox" id="newContentOnly"> New Content (24h)
                        </label>
                        <label class="checkbox-label">
                            <input type="checkbox" id="trendingOnly"> Trending Content
                        </label>
                    </div>
                </div>
                
                <div class="quick-actions">
                    <a href="/submit" class="action-btn">
                        <span>🚀</span>
                        Submit Content
                    </a>
                    <button class="action-btn secondary" id="resetFilters">
                        <span>🔄</span>
                        Reset Filters
                    </button>
                    <button class="action-btn secondary" id="refreshFeed">
                        <span>⚡</span>
                        Refresh Feed
                    </button>
                    <button class="action-btn secondary" id="exportFiltered">
                        <span>📄</span>
                        Export
                    </button>
                </div>
            </div>            <!-- Enhanced Feed Layout with Sidebar -->
            <div class="feed-layout">
                <div class="feed-main">
                    <!-- Feed View Modes -->
                    <div class="feed-view" id="gridView">
                        <div class="feed-container" id="feedContainer">
                            <% if (links && links.length > 0) { %>
                                <% links.forEach(link => { %>
                                    <%- include('../components/linkCard', { link: link }) %>
                                <% }); %>
                            <% } else { %>
                                <div class="no-content">
                                    <h3 class="glitch">No content yet</h3>
                                    <p>Be the first to share something!</p>
                                    <a href="/submit" class="submit-btn neon-text">
                                        <span class="submit-icon">🚀</span>
                                        Submit Content
                                    </a>
                                </div>
                            <% } %>
                        </div>
                    </div>
                    
                    <!-- Platform Organization View -->
                    <div class="feed-view platform-view" id="platformView" style="display: none;">
                        <div class="platform-grid" id="platformGrid">
                            <!-- Dynamically populated by JavaScript -->
                        </div>
                    </div>
                    
                    <% if (hasMore) { %>
                        <div class="load-more-container">
                            <button id="loadMoreBtn" class="load-more-btn neon-text">
                                <span class="load-icon">⬇️</span>
                                Load More
                            </button>
                            <div id="loadingSpinner" class="loading-spinner" style="display: none;">
                                <div class="spinner-ring"></div>
                                <span>Loading...</span>
                            </div>
                        </div>
                    <% } %>
                </div>
                
                <!-- Enhanced Sidebar -->
                <div class="feed-sidebar">
                    <div class="sidebar-section">
                        <h3 class="sidebar-title">Real-time Status</h3>
                        <div class="real-time-indicator">
                            <span class="status-dot" id="connectionStatus"></span>
                            <span id="connectionText">Connecting...</span>
                        </div>
                        <div class="stat-grid">
                            <div class="mini-stat">
                                <span class="mini-stat-number" id="liveCount">0</span>
                                <span class="mini-stat-label">Live</span>
                            </div>
                            <div class="mini-stat">
                                <span class="mini-stat-number" id="filteredCount">0</span>
                                <span class="mini-stat-label">Filtered</span>
                            </div>
                        </div>
                    </div>
                    
                    <div class="sidebar-section">
                        <h3 class="sidebar-title">Platform Statistics</h3>
                        <div class="platform-list" id="sidebarPlatforms">
                            <!-- Dynamically populated -->
                        </div>
                    </div>
                    
                    <div class="sidebar-section">
                        <h3 class="sidebar-title">Recent Activity</h3>
                        <div class="recent-activity" id="recentActivity">
                            <!-- Dynamically populated -->
                        </div>
                    </div>
                    
                    <div class="sidebar-section">
                        <h3 class="sidebar-title">Quality Metrics</h3>
                        <div class="quality-stats">
                            <div class="quality-item">
                                <span class="quality-label">Avg Quality</span>
                                <span class="quality-value" id="avgQuality">-</span>
                            </div>
                            <div class="quality-item">
                                <span class="quality-label">High Quality</span>
                                <span class="quality-value" id="highQualityPercent">-</span>
                            </div>
                            <div class="quality-item">
                                <span class="quality-label">Platforms</span>
                                <span class="quality-value" id="platformCount">-</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
      <!-- Scripts -->
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/voting.js"></script>
    <script src="/js/enhanced-feed.js"></script>
</body>
</html>